import React from 'react';
import { Controller} from 'react-hook-form';
import { Box, Button, TextField, Typography, Container, Grid, FormControlLabel, Checkbox, Select, MenuItem } from '@mui/material';
import { ListeningTest } from '@/utils/postListening'

const renderQuestionFields = (question: any, passageIndex: number, questionIndex: number) => {
    const lt = new ListeningTest()

    return (
        <></>
    );
  };

  export default renderQuestionFields;